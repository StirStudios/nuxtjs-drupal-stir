import {
  materialButton,
  materialSelectControl,
  materialTextControl,
} from '../theme/nuxtUi'

/** @deprecated Use the Nuxt UI theme configuration instead. */
export const materialVariant = materialTextControl
/** @deprecated Use the Nuxt UI theme configuration instead. */
export const materialVariantWithPB = materialSelectControl
/** @deprecated Use the Nuxt UI theme configuration instead. */
export const materialVariantMuted = materialButton
